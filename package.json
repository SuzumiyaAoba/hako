{
  "name": "hako",
  "private": true,
  "packageManager": "pnpm@9.12.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
